name: Deploy Eventos to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Validate JavaScript
        run: |
          node --check app.js
          node --check cloud-code.js
          node --check sw.js
      - name: Validate active build
        run: |
          grep -q "cloud-code.js" index.html
          ! grep -q "cloud-robust.js" index.html
          ! grep -q "bootstrap.js" index.html
          ! grep -q "cloud-anonymous.js" index.html
          ! grep -qi "password" cloud-code.js
          ! grep -qi "email" cloud-code.js
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
